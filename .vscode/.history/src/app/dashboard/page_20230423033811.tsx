import Button from '@/components/ui/Button'
import { FC } from 'react'

interface IpageProps {}

const page: FC<IpageProps> = ({}) => {
  return <Button size={'default'} variant={'default'} />
}

export default page
